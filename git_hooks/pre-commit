#!/usr/bin/env bash
set -eo pipefail

devtool="$(git rev-parse --show-toplevel)/git_hooks/devtool.py"
# b36datetime="$(date '+%s' |perl -lne '$a="0123456789abcdefghijklmnopqrstuvwxyz";$s="";do{$s=substr($a,$_%36,1).$s;$_=int($_/36)}while$_;printf"%07s\n",$s')"

"$devtool" verify-black
"$devtool" find-berror-dups
"$devtool" sign
